{
  "name": "rescatanimal-landing",
  "version": "0.2.0",
  "private": true,
  "description": "Rescat animal landing page - Simple vanilla HTML/CSS/JS",
  "dependencies": {
    "gh-pages": "^3.2.3"
  },
  "scripts": {
    "start": "cd public && python3 -m http.server 8080",
    "deploy": "echo 'rescatanimal.com' > ./public/CNAME && gh-pages -d public"
  }
}
